 // else {
        //     int index = s.find_first_of(first, 1); // find the first index of the first character (excluding the first position)
        //     string new_s = s.substr(index, a_count); // take all the 'a's to the beginning
        //     new_s.append(s.substr(a_count, index-a_count));
        //     new_s.append(s.substr(0, a_count));
        //     new_s.append(s.substr(index+a_count));
        //     cout << new_s << endl;
        // }